{
    "extends": "./tsconfig.lib.json",
    "include": ["tests"],

    "compilerOptions": {
        // Modules
        "paths": { "@/*": ["./dist/*"] },
        "types": ["@types/node"],

        // Emit
        "declaration": false,
        "emitDeclarationOnly": false,
        "noEmit": true
    }
}
