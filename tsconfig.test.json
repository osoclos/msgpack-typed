{
    "extends": "./tsconfig.lib.json",
    "include": ["tests"],

    "compilerOptions": {
        // Modules
        "paths": { "@/*": ["./dist/*"] },

        // Emit
        "declaration": false,
        "emitDeclarationOnly": false,
        "noEmit": true
    }
}
