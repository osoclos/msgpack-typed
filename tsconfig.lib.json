{
    "extends": "./tsconfig.json",

    "include": ["src"],
    "exclude": ["dist"],

    "compilerOptions": {
        // Modules
        "types": ["vite-plugin-wat2wasm/types"],

        // Emit
        "declaration": true,
        "emitDeclarationOnly": true,
        "noEmit": false,
        "outDir": "dist",

        // Language and Environment
        "lib": ["ES2023", "DOM"],
        "target": "ES2023"
    }
}
