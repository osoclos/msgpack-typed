{
    "extends": "./tsconfig.json",
    "include": ["tsdown.config.ts"],

    "compilerOptions": {
        // Modules
        "types": ["@types/node"],

        // Language and Environment
        "lib": ["ESNext"],
        "target": "ESNext"
    }
}
